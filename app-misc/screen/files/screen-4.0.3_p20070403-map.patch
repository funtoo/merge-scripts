diff --git a/src/termcap.c b/src/termcap.c
index 01082cb..b96497d 100644
--- a/src/termcap.c
+++ b/src/termcap.c
@@ -548,8 +548,6 @@ int map;
       else
 	break;
     }
-  if (n < KMAP_KEYS)
-    domap = 1;
   if (map == 0 && domap)
     return 0;
   if (map && !domap)
